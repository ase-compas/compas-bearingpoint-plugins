{
  "name": "scl-quality-guard",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/scl-quality-guard/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "lint": {
      "command": "eslint apps/scl-quality-guard/**/*.{ts,svelte,spec.ts}"
    },
    "check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "svelte-check",
        "cwd": "apps/scl-quality-guard"
      }
    },
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/scl-quality-guard"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "scl-quality-guard:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "scl-quality-guard:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "scl-quality-guard:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nx/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "scl-quality-guard:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "scl-quality-guard:build:development"
        },
        "production": {
          "buildTarget": "scl-quality-guard:build:production"
        }
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "../../coverage/apps/scl-quality-guard"
      }
    }
  }
}
